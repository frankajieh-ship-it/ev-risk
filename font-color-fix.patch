diff --git a/components/ListingUrlForm.tsx b/components/ListingUrlForm.tsx
index 1234567..abcdefg 100644
--- a/components/ListingUrlForm.tsx
+++ b/components/ListingUrlForm.tsx
@@ -153,12 +153,12 @@ export default function ListingUrlForm({
             onChange={(e) => setUrl(e.target.value)}
             placeholder="https://www.autotrader.com/cars-for-sale/..."
             disabled={extracting}
-            className="w-full px-3 md:px-4 py-2 md:py-3 border-2 border-gray-200 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all disabled:bg-gray-50 disabled:cursor-not-allowed text-sm"
+            className="w-full px-3 md:px-4 py-2 md:py-3 border-2 border-gray-200 rounded-lg focus:border-blue-500 focus:ring-2 focus:ring-blue-100 transition-all disabled:bg-gray-50 disabled:cursor-not-allowed text-sm text-gray-900 placeholder:text-gray-500"
           />
           {/* Trust microcopy */}
-          <p className="text-xs text-gray-500 mt-1.5">
+          <p className="text-xs text-gray-900 mt-1.5">
             No signup. We don't store the listing URL.
           </p>
         </div>
 
         {error && (
@@ -203,10 +203,10 @@ export default function ListingUrlForm({
         {/* Helper text under button */}
         {!extracting && !error && (
           <div className="space-y-1">
-            <p className="text-xs text-gray-600">
+            <p className="text-xs text-gray-900">
               We'll extract vehicle details and flag what usually surprises buyers.
             </p>
-            <p className="text-xs text-gray-500">
+            <p className="text-xs text-gray-800">
               If we can't parse it, we'll ask for 3 quick details.
             </p>
           </div>
